This folder is the home for the generated iOS xcframework produced from the Rust FFI crate.

Generate it with:

  ./ios/build-ffi.sh

The script will place `OoniAuthFFI.xcframework` in this directory.
